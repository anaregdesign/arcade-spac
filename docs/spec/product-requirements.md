# Arcade App 要件整理

## Summary

複数のパズルゲームを 1 つのアカウントと共通 UI で横断し、ゲーム別の上達と総合的な強さを継続的に確認できる組織内向けゲームポータルを定義する。

## User Problem

- 単体ゲームごとに遊び場や成績確認の導線が分かれると、継続利用しづらい
- 生のゲーム結果だけでは、複数ゲームを横断した総合的な実力が見えにくい
- 競争要素があっても、自分の現在地、次に伸ばすべきゲーム、結果の共有先が分断されると体験が弱くなる

## Scope

- `Microsoft Entra ID` ログインを前提にした組織内ユーザ向けゲームポータル
- `マインスイーパ` と `数独` の 2 ゲーム
- ホーム、ゲーム、結果、ランキング、プロフィール、初回案内の主要体験
- ゲーム別ランキング、総合ランキング、総合得点、プレイ履歴、`Microsoft Teams` 共有

## Non-Goals

- フレンド機能やチャットなどのソーシャル機能
- リアルタイム対戦やチーム戦
- デイリー、ウィークリーなど MVP 外の追加ランキング期間
- マルチテナント対応

## Acceptance Criteria

- ユーザは `Microsoft Entra ID` でサインインし、初回利用時はホーム画面上の案内を経て、そのまま `マインスイーパ` または `数独` を開始できる
- ユーザはホーム画面、ゲーム画面、結果画面のいずれからでも次の行動を判断でき、ゲーム間を迷わず切り替えられる
- プレイ結果画面では、今回の記録、自己ベスト差分、ランキングまたは総合得点への影響、`集計待ち` や `保存保留` などの状態、再挑戦と共有導線を共通レイアウトで確認できる
- ランキング画面では、`総合` と `ゲーム別`、`シーズン` と `累計` を切り替えても自分の順位と差分を見失わない
- プロフィール画面では、ゲーム別ベスト、総合得点の内訳、成長推移、表示名と公開範囲を確認または変更できる
- 進行中プレイから離脱する場合、共有リンクを未認証で開く場合、結果保存が失敗する場合のいずれでも、ユーザは次に取るべき行動を画面上で理解できる

## Constraints and Dependencies

- MVP の認証対象は `AzureADMyOrg` を前提とした同一テナント内ユーザに限定する
- 共有リンク、ランキング詳細、プロフィール閲覧は同一テナントの認証済みユーザだけが利用できる
- スコア登録、ランキング集計、プロフィール保持を扱うため、静的配信だけではなくサーバランタイムを前提にする
- MVP の対象ゲームは 2 つ、対象期間は `シーズン` と `累計` に限定する

## Links

- Flow: [screen-flow.md](./screen-flow.md)
- Execution plan: [../plan.md](../plan.md)

## 1. このアプリが提供する体験

このアプリは、1 人のユーザが複数のパズルゲームを 1 つのアカウントで横断して遊び、各ゲームの成績と総合成績の両方で他ユーザと競えるゲームポータルである。

初期対象ゲームは次を想定する。

- マインスイーパ
- 数独

ユーザは `Microsoft Entra ID` でログインし、自分のプレイ履歴、ゲーム別スコア、総合得点、ランキング推移を確認しながら、ホーム画面と各ゲーム画面のどちらからでも別ゲームへスムーズに移動できる。

このアプリの価値は次の 3 点にある。

- 複数ゲームを 1 つの導線で遊べること
- ゲーム別の上達と総合的な強さを両方見られること
- 他ユーザとの比較結果がわかりやすく可視化されること

## 2. 想定ユーザ

### 2.1 メインユーザ

- 日常的に短時間でパズルゲームを遊びたいユーザ
- 同じゲームを繰り返し練習し、ランキング上位を狙いたいユーザ
- 1 つのゲームに偏らず、複数ゲームを横断して総合力を高めたいユーザ

### 2.2 ユーザの期待

- ログイン後すぐに何で遊ぶか選べること
- いま自分が強いゲームと弱いゲームがわかること
- 1 回ごとの結果だけでなく、継続的な成長が見えること
- 他人と比べてどの位置にいるかがすぐわかること
- ゲームを変えるたびにアプリの文脈が切れないこと

## 3. 主要ユースケース

### 3.1 初回利用

1. ユーザはログイン画面を開く。
2. `Microsoft Entra ID` でサインインする。
3. 初回ログイン時はプロフィールの初期作成が行われる。
4. ログイン直後にホーム画面兼ゲーム選択画面へ遷移する。
5. 初回利用時はホーム画面上で簡潔な初回案内を自動表示する。案内はスキップ可能だが、初回プレイ前に一度は視認できるようにする。
6. ユーザは案内を閉じたうえでマインスイーパまたは数独を選んでプレイを開始する。

### 3.2 日常利用

1. ユーザはログインする。
2. ホーム画面で総合順位、各ゲームの自己ベスト、直近の成績推移を確認する。
3. 遊びたいゲームを選ぶ。
4. プレイ結果を確認する。
5. 結果画面またはゲーム内ナビゲーションから別ゲームへ移動する。
6. その日のうちに複数ゲームを切り替えながら遊ぶ。

### 3.3 競争利用

1. ユーザはあるゲームのランキングを見る。
2. 自分より上位のユーザとの差分を確認する。
3. 自己ベスト更新を狙って再挑戦する。
4. 必要に応じて別ゲームへ移動し、総合得点の底上げを狙う。
5. 総合ランキング画面で、複数ゲームの総合力としての自分の順位を確認する。

## 4. 画面とユーザ行動の整理

### 4.1 ログイン画面

#### ユーザがやりたいこと

- 安全にログインしたい
- ログイン後に何ができるかを理解したい

#### 必須要件

- `Microsoft Entra ID` によるサインイン導線を提供する
- 未ログイン状態ではランキングの詳細閲覧やスコア登録を制限する
- ログイン成功後はホーム画面へ遷移する
- ログイン失敗時は理由がわかるメッセージを表示する

#### 表示要素

- アプリ名と短い説明
- サインインボタン
- 未ログイン状態で見られる範囲の説明
- 利用規約やプライバシーへの導線

#### 追加で必要な要件

- 前回ログイン済みのユーザには、不要な再ログインを避けて自然にホームへ復帰できること
- 初回利用者には、ホーム画面上で簡単な使い方案内を自動表示できること

### 4.2 ホーム画面兼ゲーム選択画面

#### ユーザがやりたいこと

- 次に遊ぶゲームを選びたい
- 自分の現在地を一目で知りたい
- どのゲームで得点を伸ばせそうか判断したい

#### 必須要件

- ログイン直後のランディング画面とする
- プレイ可能なゲームをカード一覧で表示する
- 各ゲームカードから直接ゲーム画面へ遷移できる
- 総合得点、総合順位、直近プレイ、自己ベストを要約表示する
- 各ゲームの現在順位と自己ベストを、未確定時の表示ルールも含めて一目で比較できる

#### 表示要素

- 総合得点カード
- 総合順位カード
- 各ゲームのカード
- 直近プレイ履歴
- おすすめ導線: 「あと少しで自己ベスト更新」「このゲームで総合得点を伸ばせる」

#### 追加で必要な要件

- まだ一度もプレイしていないユーザ向けに、空状態でも次の行動がわかること
- 初回利用時は、どのゲームから始めるべきか迷わない導線を、ホーム上の初回案内または強調表示で出すこと
- プレイ履歴が少ない段階でも、総合得点やランキングが未確定であることをわかりやすく伝えること

### 4.3 ゲーム画面

#### ユーザがやりたいこと

- いま遊んでいるゲームに集中したい
- 途中で別のゲームへ切り替えたい
- 現在のプレイがランキングにどう影響するか知りたい

#### 必須要件

- ゲーム画面は極力画面全体を利用し、プレイ領域を最大化する
- 現在のゲーム名と難易度を明示する
- 操作方法は別画面に分離せず、ゲーム画面と同じ画面内で常に到達可能にする
- 操作説明は初回利用時だけでなく、再訪時もすぐ視認できる位置に置く
- ホームへ戻る導線を常に表示する
- 他ゲームへ直接遷移できるゲーム切り替え導線を表示する
- プレイ終了後は結果画面を表示する
- 再挑戦を素早く促す場合に限り、結果モーダルを補助的に使ってよい。ただし情報構成は結果画面と同等にする
- プレイ中断時の扱いを定義する
- 途中でホームや他ゲームへ移動する場合は、進行中プレイが失われるかどうかを事前に明示する
- 進行中プレイからホームや他ゲームへ移動する場合は、確認ダイアログを表示する
- 確認して離脱したプレイは `中断` として履歴に残してよいが、ランキングと総合得点の集計対象外とする
- プレイ完了後に保存失敗した場合は、結果を `保存保留` として保持し、再試行導線と未確定状態を表示する

#### 表示要素

- ゲーム本体
- 同一画面内で常に到達可能な操作方法ガイド
- タイマーまたは進行状況
- 難易度選択
- ホームへの導線
- 他ゲーム切り替えメニュー
- 自己ベストとの差分表示

#### レイアウト方針

- プレイ領域を主役にし、周辺 UI は邪魔にならない最小限の密度で配置する
- 操作方法ガイドは折りたたみ可能でもよいが、存在自体は常に明示する
- モバイルでもデスクトップでも、重要操作と操作方法が同じ画面内で完結すること
- 横向きや広い画面ではプレイ領域を広く使い、狭い画面では説明や補助情報を再配置してもゲーム体験を阻害しないこと

#### 追加で必要な要件

- 一時停止の有無と、そのときタイマーやランキング対象条件がどう変わるかを明示すること
- 通信失敗時や保存失敗時にも、ユーザが現在のプレイ状態を理解できること

### 4.4 プレイ結果画面

#### ユーザがやりたいこと

- 今回の結果が良かったのかすぐ知りたい
- ランキングや総合得点への影響を確認したい
- そのまま再挑戦または別ゲームへ移動したい

#### 必須要件

- 今回の記録を明示する
- 全ゲームで共通の結果画面スタイルを用いる
- 自己ベスト更新の有無を表示する
- ゲーム別ランキングの暫定順位変動、またはランキング対象外理由を表示する
- 総合得点の暫定増減を表示する
- 集計確定前は、順位と総合得点が暫定値であることを明示する
- `保存保留` 時は、確定前であることと再試行導線を表示する
- `Microsoft Teams` で結果を共有できるリンクを配置する
- 再プレイと他ゲーム遷移の両方を提供する

#### 表示要素

- 今回スコア
- 自己ベストとの差
- 総合得点の増減または未確定表示
- 順位変動またはランキング対象外表示
- Microsoft Teams 共有リンク
- 再挑戦ボタン
- 他ゲームへ移動するボタン

#### 結果共有の要件

- 共有リンクは結果画面内で視認しやすい位置に配置する
- 共有内容には少なくともゲーム名、今回の結果、自己ベスト更新有無、総合得点または順位変動を含める
- 共有時にゲームごとの表示差は許容するが、レイアウトと導線は共通化する
- 共有導線は再挑戦や他ゲーム移動の主要導線を邪魔しない配置にする
- 共有リンク先は、同一テナントの認証済みユーザのみ閲覧できること
- 未認証で共有リンクを開いた場合は、サインイン後に該当結果画面へ復帰できること
- 共有文面とリンク先では、内部 ID や `Microsoft Entra ID` の実名を自動露出せず、必要に応じてアプリ内表示名のみを用いること

#### 推奨する共通レイアウト

Result 画面は全ゲームで次の 4 ブロック構成を共通化する。

1. 上部サマリー帯
2. 今回結果の主要指標エリア
3. 影響サマリーエリア
4. アクションエリア

##### 1. 上部サマリー帯

- ゲーム名
- 難易度
- クリア可否
- 自己ベスト更新バッジ

ここは全ゲームで同じ高さ、同じ情報密度、同じタイポグラフィ階層を使う。

##### 2. 今回結果の主要指標エリア

- 今回の記録
- ゲーム固有の主要指標 1 から 2 個
- 自己ベストとの差分

ゲーム固有の違いはこのエリアだけで吸収する。

例:

- マインスイーパ: クリア時間、ミス回数
- 数独: クリア時間、ヒント使用回数

##### 3. 影響サマリーエリア

- ゲーム別順位変動
- 総合得点の増減
- 総合順位の変動

ここはカード 3 枚または横並びの統一 KPI 表示を推奨する。

##### 4. アクションエリア

- 再挑戦
- 他ゲームへ移動
- ホームへ戻る
- Microsoft Teams で共有

アクション順は次を推奨する。

1. 再挑戦
2. Microsoft Teams で共有
3. 他ゲームへ移動
4. ホームへ戻る

#### 推奨する Result 画面の見せ方

- 主要数値は画面上部から 1 スクロール以内に収める
- 重要な変化は色だけでなく、矢印、ラベル、プラスマイナス記号でも表現する
- Microsoft Teams 共有リンクはアクションエリア内に固定し、全ゲームで同じ位置に置く
- モバイルでは縦積み、デスクトップでは 2 段または横並びに再配置する
- 共有リンクはアイコンだけにせず、`Microsoft Teams で共有` の文言を併記する

#### Microsoft Teams 共有文面の推奨テンプレート

`Microsoft Teams` 共有は、短く、競争結果がすぐ伝わり、アプリに戻りたくなる文面を優先する。

##### 推奨テンプレート A

`Arcade で {ゲーム名} {難易度} をプレイしました。今回の結果は {今回の記録}、{自己ベスト更新文}。総合得点 {総合得点}、総合順位 {総合順位} 位です。結果を見る: {結果URL}`

##### 推奨テンプレート B

`{ゲーム名} {難易度} の結果: {今回の記録}。{順位変動文} {総合得点変動文} Arcade で他のゲームにも挑戦する: {結果URL}`

##### テンプレート選定方針

- MVP ではテンプレート A を標準とする
- 自己ベスト更新時は `自己ベスト更新` を優先表示する
- 順位上昇時は順位変動文を優先表示する
- 失敗プレイや未クリア時は過度にネガティブな文言を避け、再挑戦を促す文面へ切り替える
- `中断` または `保存保留` の結果は共有対象外とする

#### Microsoft Teams 共有文面の差し込み値

##### 必須項目

- ゲーム名
- 難易度
- 今回の記録
- 自己ベスト更新有無
- 総合得点
- 総合順位またはゲーム別順位
- 結果画面へ戻れる URL

##### 推奨項目

- 順位変動
- 総合得点の増減
- プレイ日時

##### 非推奨項目

- 内部 ID
- 詳細すぎる計算途中値
- 個人情報性の高い識別子
- `Microsoft Entra ID` の実名やメールアドレス

#### 共有対象の値のおすすめ定義

MVP では、共有対象として次の値を固定採用することを推奨する。

- ゲーム名
- 難易度
- 今回の代表記録 1 件
- 自己ベスト更新有無
- ゲーム別順位変動
- 総合得点
- 総合順位
- 結果詳細へのディープリンク

#### ゲーム別の代表記録のおすすめ

- マインスイーパ: `クリア時間`
- 数独: `クリア時間`

補助指標として次を表示してよい。

- マインスイーパ: ミス回数
- 数独: ヒント使用回数

代表記録を両ゲームで `クリア時間` にそろえることで、Result 画面と共有文面の理解コストを下げる。

### 4.5 ランキング画面

#### ユーザがやりたいこと

- 自分がどのくらい強いか知りたい
- 上位ユーザとの差を把握したい
- ゲームごとの順位と総合順位を見分けたい

#### 必須要件

- ゲーム別ランキングと総合ランキングを切り替えられる
- MVP では `シーズン` と `累計` の期間を切り替えられる
- 自分の順位を固定表示する
- 上位ユーザとの差分と、自分の前後数名との差分を表示する
- フィルタを過剰に増やさず、直感的に使えること
- 自分の行を一覧の中で見失わないこと

#### 追加で必要な要件

- ユーザ名、ゲーム、期間を切り替えても、自分の現在地がすぐわかること
- 上位だけでなく、自分の前後数名との差も見えること
- 表示名の公開範囲や匿名化ルールをユーザが理解できること

#### MVP で提供する期間単位

- シーズン
- 累計

#### 拡張候補の期間単位

- デイリー
- ウィークリー
- シーズン内サブイベント

### 4.6 プロフィール画面

#### ユーザがやりたいこと

- 自分の得意不得意を知りたい
- 成長推移を見たい
- どのゲームを遊ぶと総合得点を伸ばしやすいか判断したい

#### 必須要件

- ゲーム別のベスト記録を一覧できる
- 成績推移を可視化する
- 総合得点の内訳を見られる
- プレイ回数や継続日数などの活動量を確認できる
- アプリ内表示名を確認、変更できる
- 公開される成績範囲と表示名の公開ルールを確認できる

#### 追加で必要な要件

- `Microsoft Teams` 共有やランキングに出る表示名が何になるか確認できること
- 自分の公開される成績範囲を理解できること
- サインアウト導線へ到達しやすいこと

### 4.7 初回案内とヘルプ

#### ユーザがやりたいこと

- 最初に何をすればいいか知りたい
- ゲームのルールや競争ルールをすぐ確認したい

#### 必須要件

- 初回ログイン時に、ゲーム選択、総合得点、ランキングの見方を短く案内する
- 初回案内はホーム画面上のオーバーレイ、ドロワー、またはモーダルとして表示し、案内完了後は通常のホーム操作へ戻れること
- 各ゲームのルールと操作方法をいつでも再確認できる
- 総合得点の考え方を、数式だけでなく文章でも説明する

#### 表示要素

- 初回ガイド
- ルール説明
- ランキング説明
- 総合得点の説明

## 5. スコア設計

ゲームが異なると生のスコアの意味が異なるため、ユーザ向けには次の 2 層で扱う。

- ゲーム固有の記録
- 横断比較のための競技ポイント

### 5.1 ゲーム固有の記録

ゲームそのものの魅力を保つため、生の結果はゲームに合った形で保存する。

#### マインスイーパ

- クリア時間
- 難易度
- ミス回数
- クリア可否

#### 数独

- クリア時間
- 難易度
- ミス回数
- ヒント利用回数
- クリア可否

### 5.2 競技ポイント

異なるゲーム間で比較可能にするため、各プレイ結果を 0 以上の競技ポイントへ変換する。

#### 基本方針

- ゲーム別ランキングは、生の記録と競技ポイントの両方を見せる
- 総合ランキングは、競技ポイントを用いる
- ユーザにとって理解しやすい計算式にする
- 難易度が高い挑戦ほど高得点を狙いやすくする
- 不正に得点を稼ぎにくい設計にする

#### 初期案

各プレイの競技ポイントは次で算出する。

`競技ポイント = 難易度基礎点 × クリア係数 × 品質係数`

#### 係数案

- 難易度基礎点: Easy, Normal, Hard, Expert のような段階別固定値
- クリア係数: クリアなら 1、未クリアなら 0
- 品質係数: 時間、ミス、ヒント利用を反映する 0 から 1 の係数
- 未クリアまたは `中断` プレイは履歴には残すが、ランキング集計対象外とし、結果画面では `ランキング対象外` と表示する

### 5.3 総合得点

総合得点は、各ゲームの強さを横断的に比較できるように、ゲーム別のベスト競技ポイントを合算して算出する。

#### 初期案

`総合得点(対象期間) = 各ゲームの対象期間内自己ベスト競技ポイントの合計 + 対象期間内の活動ボーナス`

MVP では、総合得点と総合ランキングの対象期間を `シーズン` と `累計` に限定する。

#### 活動ボーナスの考え方

- 毎日遊ぶ動機になる程度に小さく付与する
- 実力差を壊さないよう上限を設ける
- 連続ログインではなく、実際の有効プレイを条件にする

#### この案の意図

- 1 つのゲームだけ極端に強いユーザと、複数ゲームで安定して強いユーザを両立して評価できる
- 新しいゲームが増えても総合得点体系を拡張しやすい

### 5.4 ランキング登録対象

公平性のため、ランキング対象プレイには条件を設ける。

- 正式モードのみ対象とする
- 練習モードはランキング対象外とする
- 異常終了や切断時の扱いを定義する
- ヒント利用の有無によって別ランキングとするか、減点対象とするかを決める
- 未クリアプレイは履歴保存のみ行い、ランキングと総合得点には反映しない
- `中断` プレイは履歴保存のみ行い、ランキングと総合得点には反映しない
- `保存保留` の結果はサーバ受理完了まで集計に反映しない

## 6. 可視化要件

可視化は派手さよりも、ユーザがすぐ状況を判断できることを優先する。

### 6.1 必須の可視化

- 総合得点の推移
- ゲーム別ベスト記録比較
- ゲーム別順位または上位との差分の比較
- 直近プレイの成績推移

### 6.2 推奨チャート

- 時系列推移: 折れ線グラフ
- ゲーム別比較: 棒グラフまたはカード比較
- 順位変動: 折れ線グラフまたは順位推移表
- 総合得点の内訳: 積み上げ棒グラフまたはゲーム別一覧表

### 6.3 表示ルール

- グラフの意味はタイトルと凡例だけで理解できること
- 重要な数値はホバーしなくても見えること
- グラフの近くにテキスト要約を置くこと
- 正確な値を見たいユーザ向けに表形式の代替表示を用意すること
- 色だけで差を表現しないこと

### 6.4 状態別の表示要件

- ローディング中は、データ取得中であることが明確にわかること
- データがまだ存在しない場合は、空状態として次の行動を案内すること
- 通信失敗や保存失敗時は、何が失敗したかと再試行方法がわかること
- ランキング集計中や共有リンク生成中など、待ち時間のある処理では進行状態を伝えること

### 6.5 共通状態ラベル

- `未プレイ`: 対象ゲームまたは対象期間に有効プレイがまだ存在しない状態
- `集計待ち`: 結果は受理されたが、ランキングや総合得点への反映が未完了の状態
- `ランキング対象外`: 練習モード、未クリア、中断などで順位計算の対象外である状態
- `保存保留`: クライアントには結果があるが、サーバ保存が完了していない状態

### 6.6 ダッシュボードの見せ方

ホーム画面では次を優先表示する。

- 総合得点
- 総合順位
- 各ゲームの最新順位または自己ベスト
- 直近の上昇傾向

プロフィールまたは詳細画面では次を表示する。

- ゲーム別の強さ比較
- 期間別の成長推移
- 総合得点の内訳
- 自分と上位ユーザとの差分

## 7. ナビゲーション要件

このアプリでは「いま何を遊ぶか」を頻繁に切り替えるため、画面遷移は特に重要である。

### 必須要件

- ログイン後は必ずホーム画面に入り、初回利用時のみその上で初回案内を表示する
- ホーム画面から任意のゲームへ 1 アクションで入れる
- 各ゲーム画面から他ゲームへ直接移動できる
- 各ゲーム画面からホームへ戻れる
- プレイ結果画面から再挑戦、他ゲーム移動、ホーム復帰のいずれもできる

### ナビゲーションの基本構造案

- ログイン画面
- ホーム兼ゲーム選択画面
- ゲーム画面
- プレイ結果画面
- ランキング画面
- プロフィール画面

### 7.1 ゲーム横断 UI 方針

複数ゲームを切り替えても、ユーザが同じアプリの中にいると直感的にわかるように、ゲーム横断で統一した UI を提供する。

#### 必須要件

- 全ゲームで共通のヘッダ、戻る導線、ゲーム切り替え導線、結果表示ルールを用いる
- 全ゲームで同じテーマカラー体系を用いる
- ボタン、カード、ダイアログ、ランキング表示、結果表示の見た目と操作感を統一する
- 結果画面の共有導線は全ゲームで同じ位置と同じ操作感にそろえる
- ゲームごとの差はゲーム盤面やルール説明など、本質的に必要な箇所に限定する

#### テーマ方針

- アプリ全体で 1 つの基調テーマを持つ
- ゲームごとに差を付ける場合も、同一テーマの範囲内でアクセント差に留める
- 総合ランキング、ゲーム別ランキング、ホーム、ゲーム画面のすべてで同系統の配色ルールを使う
- 色だけに依存せず、ラベル、アイコン、位置関係でも意味が伝わること

## 8. 認証とユーザ管理要件

### 8.1 認証

- 認証は `Microsoft Entra ID` を用いる
- このアプリはスコア登録、ランキング参照、プロフィール保持を伴うため、ブラウザだけで閉じた純粋な静的 SPA ではなく、サーバランタイムを持つ前提で整理する
- 認証後はアプリ内セッションで利用状態を維持する

### 8.2 ユーザプロフィール

- `Microsoft Entra ID` の一意なユーザ識別子を内部ユーザと紐付ける
- 表示名、アイコン、参加日、総合得点、ゲーム別記録を保持する
- ランキング、プロフィール、共有リンク先では、`Microsoft Entra ID` の実名ではなくアプリ内表示名を標準で用いる
- アプリ内表示名は初回ログイン時に Entra の表示名から初期化してよいが、ユーザが後から変更できること
- ランキング上の表示名変更ポリシーを定義する

### 8.3 競争の前提

- 同一ユーザの重複アカウント運用をどこまで許容するか整理する
- MVP は `AzureADMyOrg` を前提に組織内ユーザ向けに始める
- 共有リンク、ランキング詳細、プロフィール閲覧は同一テナントの認証済みユーザに限定する
- 将来のマルチテナント対応は拡張項目として扱う

### 8.4 セッションとサインアウト

- ユーザは明示的にサインアウトできること
- 一定期間内の再訪では、可能な限りログイン状態を維持できること
- セッション切れ時は唐突に失敗させず、再ログインが必要であることを伝えること
- セッション切れがプレイ結果登録に影響する場合は、その結果をユーザに明示すること

## 9. データとして持つべき情報

### ユーザ

- ユーザ ID
- Entra ID のオブジェクト ID
- 表示名
- アイコン URL
- 登録日

### ゲーム定義

- ゲーム ID
- ゲーム名
- 対応難易度
- ランキング対象条件

### プレイ結果

- プレイ ID
- ユーザ ID
- ゲーム ID
- 難易度
- 開始日時
- 終了日時
- プレイ状態 (`完了` / `未クリア` / `中断` / `保存保留`)
- クリア可否
- 生の記録
- 算出後の競技ポイント
- ランキング対象可否

### 集計情報

- ゲーム別自己ベスト
- ゲーム別順位またはゲーム別ベスト到達順位
- 総合得点
- 総合順位
- 期間別スコア集計

## 10. MVP の範囲

最初のリリースでは、体験の軸を崩さずに次へ絞る。

- `Microsoft Entra ID` ログイン
- マインスイーパと数独の 2 ゲーム
- ホーム画面上での初回案内とヘルプ導線
- ホーム画面からのゲーム選択
- ゲーム画面からの他ゲーム遷移
- シーズンと累計に対応したゲーム別ランキング
- シーズンと累計に対応した総合ランキング
- 総合得点の可視化
- 自分のプレイ履歴の可視化
- プロフィール画面
- `Microsoft Teams` 共有

## 11. MVP では後回しにできるもの

- フレンド機能
- チャット機能
- ギルドやチーム戦
- 実績バッジの詳細設計
- 高度なレコメンド
- リアルタイム対戦
- デイリー、ウィークリーランキング
- マルチテナント対応

## 12. ユーザ体験上の重要ポイント

- ログイン後に迷わせないこと
- 「いま何を遊ぶか」と「いま自分がどれだけ強いか」がすぐわかること
- ゲーム画面では盤面やプレイ領域を最大限広く取りつつ、操作方法が同じ画面で一目でわかること
- 結果が更新された瞬間に、自己ベスト、順位、総合得点の変化と、その値が暫定か確定かを理解できること
- 複数ゲームをまたいでも同じアプリを使っている一体感があること
- どのゲームへ移動しても UI とテーマカラーが統一され、学習コストが増えないこと
- 可視化が多すぎて判断しづらくならないこと
- 初回利用時でも、ルール、競争方式、次に取るべき行動が迷わず理解できること
- 通信失敗やセッション切れが起きても、ユーザが状況を理解して復帰できること

## 13. 現時点で決めるべき未確定事項

実装前に、次の点をプロダクト判断として確定させる必要がある。

1. 競技ポイントの `品質係数` をゲームごとにどう数式化するか
2. シーズンの長さと切り替えタイミングをどう定義するか
3. 数独のヒント利用を減点扱いにするか、別モード扱いにするか
4. 初期リリースで可視化をどこまで載せるか
5. デイリー、ウィークリーランキングをいつ追加するか

## 14. 推奨する次の仕様化ステップ

1. マインスイーパと数独それぞれの `品質係数` とランキング対象条件を定義する
2. ランキングとプレイ結果のデータモデルに `中断` と `保存保留` を反映する
3. 初回案内、結果画面、プロフィール画面のワイヤーフレームを作る
4. `Microsoft Entra ID` のアプリ登録、セッション維持、共有リンク復帰フローを確定する
5. シーズン運用ルールと集計バッチの反映タイミングを決める
