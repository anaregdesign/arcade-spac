name: arcade

metadata:
  template: azure-app-platform-delivery

services:
  web:
    project: .
    host: containerapp
    language: js
    docker:
      path: ./Dockerfile

hooks:
  postprovision:
    shell: sh
    run: ./scripts/azure/postprovision.sh
    continueOnError: false